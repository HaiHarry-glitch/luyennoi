.env
.env.local
.local-data/
node_modules/

# Dev / scraped data (not needed for deploy)
scraped/
LuyenNoi/
.claude/

# Plan files
PLAN-*.md
INTEGRATION-PLAN.md
plan.md

# Logs
*.log
*.txt
!package.json

# Images not needed in deploy (only ignore root copies; public/ ones are kept)
/QRCode.png
/hin-logo.png

# (netlify/ and netlify.toml included for auto-deploy)

# Dev scripts (only keep export script for Netlify build)
scripts/*
!scripts/export-netlify-static.js

# Supabase SQL (not needed at runtime)
supabase/

# Package lock not needed (no deps)
package-lock.json

# Backup files
*.before-*
public/real/home-netlify.html
